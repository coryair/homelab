apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-config
  namespace: homepage
data:
  settings.yaml: |
    title: Homepage
  allowedHosts: |
    - homepage.home.coryair.com
  services.yaml: |
    - Group:
       - OPNSense
        href: http://router.home.coryair.com
  widgets.yaml: |
    []
